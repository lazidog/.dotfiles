#!/usr/bin/env bash

# Install command-line tools using Homebrew.

# Make sure we’re using the latest Homebrew.
brew update

# Upgrade any already-installed formulae.
brew upgrade


brewFormulae="git \
    python@3 \
    neovim \
    tmux \
    ripgrep \
    direnv \
    node \
    "

brewCask="iterm2 \
    visual-studio-code \
    karabiner-elements \
    sublime-text \
    "

printf "Installing homebrew stuff\n"
brew install $brewFormulae
brew install --cask $brewCask

brew tap homebrew/cask-fonts
brew install --cask font-hack-nerd-font

# Save Homebrew’s installed location.
BREW_PREFIX=$(brew --prefix)

# Install `wget` with IRI support.
# brew install wget --with-iri

# Install GnuPG to enable PGP-signing commits.
# brew install gnupg

# Install more recent versions of some macOS tools.
# brew install vim --with-override-system-vi
# brew install grep

# Remove outdated versions from the cellar.
brew cleanup

# brew list
# ==> Formulae
# aom                     freetype                libpq                   mpdecimal               ripgrep
# apr                     gd                      libsodium               msgpack                 rtmpdump
# apr-util                gettext                 libssh2                 ncurses                 ruby
# argon2                  gh                      libtermkey              neovim                  shopify-cli
# autoconf                giflib                  libtiff                 net-snmp                sqlite
# awscli                  git                     libtool                 node                    supabase
# bash                    gmp                     libunistring            nspr                    terminal-notifier
# brotli                  highway                 libuv                   nss                     terraform
# c-ares                  icu4c@76                libvmaf                 oniguruma               tidy-html5
# ca-certificates         imath                   libvterm                openexr                 tmux
# cffi                    jpeg-turbo              libyaml                 openldap                tree
# cocoapods               jpeg-xl                 libzip                  openssl@3               tree-sitter
# cryptography            krb5                    little-cms2             pcre2                   unibilium
# curl                    libavif                 luajit                  php                     unixodbc
# direnv                  libdeflate              luv                     pycparser               utf8proc
# fastlane                libevent                lz4                     python@3.11             webp
# fontconfig              libnghttp2              m4                      python@3.12             xz
# freetds                 libpng                  mkcert                  readline                zstd

# ==> Casks
# font-hack-nerd-font     iterm2                  karabiner-elements      ngrok                   visual-studio-code
